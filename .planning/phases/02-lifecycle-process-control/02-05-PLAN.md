---
phase: 02-lifecycle-process-control
plan: 05
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - src/client/components/ProjectFormDrawer.tsx
  - tests/client/ProjectFormDrawer.test.tsx
autonomous: true
requirements:
  - LIFE-01
  - LIFE-04
must_haves:
  truths:
    - "D-01: Project creation uses a directory path input plus Browse control."
    - "D-02: Directory selection or path entry loads package.json scripts from the backend."
    - "D-03: User selects exactly one script per project."
    - "D-04: Old Phase 1 fields remain in schema but are hidden from create/edit UI."
    - "D-18: Submit payload stores scriptName alongside hostPath and derives startCommand from the selected script."
    - "LIFE-04: Package parsing and missing-path errors are visible in the form."
  artifacts:
    - path: "src/client/components/ProjectFormDrawer.tsx"
      provides: "Directory picker, Browse button, script dropdown, hidden old fields, derived payload"
    - path: "tests/client/ProjectFormDrawer.test.tsx"
      provides: "Component coverage for directory/script form workflow"
  key_links:
    - from: "src/client/components/ProjectFormDrawer.tsx"
      to: "src/client/api/projectsApi.ts"
      via: "parseScripts(path)"
      pattern: "parseScripts"
    - from: "src/client/components/ProjectFormDrawer.tsx"
      to: "src/shared/projectSchema.ts"
      via: "projectInputSchema validation before submit"
      pattern: "projectInputSchema"
---

<objective>
Rebuild the project form around directory selection and npm script selection.

Purpose: Implement D-01 through D-04 and D-18 in executable frontend planning so project creation produces a valid lifecycle target without exposing old Phase 1 fields.
Output: Updated ProjectFormDrawer and tests.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-lifecycle-process-control/02-CONTEXT.md
@.planning/phases/02-lifecycle-process-control/02-RESEARCH.md
@.planning/phases/02-lifecycle-process-control/02-PATTERNS.md
@.planning/phases/02-lifecycle-process-control/02-UI-SPEC.md
@.planning/phases/02-lifecycle-process-control/02-VALIDATION.md
@.planning/phases/02-lifecycle-process-control/02-03-SUMMARY.md
@AGENTS.md

Research open-question resolution: Use a visible Material UI text field for manual path entry plus a Browse button backed by hidden `input type="file"` with `webkitdirectory`. Derive and store `startCommand` as `npm run ${scriptName}` at submit time while also storing `scriptName`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace old form fields with directory picker and script dropdown</name>
  <files>src/client/components/ProjectFormDrawer.tsx, tests/client/ProjectFormDrawer.test.tsx</files>
  <behavior>
    - Test 1: Add form renders Name, Directory path, Browse, Script, Cancel, and Add project controls.
    - Test 2: Container path, app URL, port, health URL, env file, env vars, and autostart controls do not render.
    - Test 3: typing a directory path calls parseScripts after the configured debounce or after a Check path action if that local pattern is chosen.
    - Test 4: Browse selection populates directory path and calls parseScripts.
    - Test 5: scripts returned by parseScripts populate the Script dropdown.
  </behavior>
  <action>Update `ProjectFormDrawer` to keep the existing Material UI Drawer shell but replace Phase 1 field-by-field inputs with the Phase 2 flow: Name, Directory path TextField, Browse Button, hidden directory file input using `webkitdirectory`, and a Script Select or Autocomplete populated by `parseScripts`. Use `FolderOpen` icon for Browse. The path text field must support manual entry. The browse implementation may use `webkitRelativePath` when available; when a browser cannot expose an absolute path, keep the text input as the authoritative manual path. Hide old fields per D-04 rather than deleting their schema support.</action>
  <verify>
    <automated>npx vitest run tests/client/ProjectFormDrawer.test.tsx --reporter=verbose</automated>
  </verify>
  <done>The form presents only the directory and script workflow and hides old Phase 1 fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Submit derived lifecycle-ready project payload</name>
  <files>src/client/components/ProjectFormDrawer.tsx, tests/client/ProjectFormDrawer.test.tsx</files>
  <behavior>
    - Test 1: submitting with selected script `dev` sends `scriptName: "dev"`.
    - Test 2: submit payload derives `startCommand: "npm run dev"`.
    - Test 3: submit payload sets `hostPath` and `containerPath` from the directory path.
    - Test 4: optional hidden fields are omitted or set to existing safe defaults accepted by projectInputSchema.
    - Test 5: attempting to submit without a selected script shows a field error and does not call create/update.
  </behavior>
  <action>Revise the form state and payload builder so the selected directory is used for `hostPath` and `containerPath`, selected script is saved as `scriptName`, and `startCommand` is derived as `npm run ${scriptName}`. Validate the payload with the existing `projectInputSchema` before calling create/update. Preserve edit mode by loading existing `hostPath` and `scriptName`, deriving the dropdown value, and allowing scripts to be refreshed from the current directory.</action>
  <verify>
    <automated>npx vitest run tests/client/ProjectFormDrawer.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Project create/edit produces the path plus exactly one script selection required by lifecycle start/restart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface parse and missing-path errors in the form</name>
  <files>src/client/components/ProjectFormDrawer.tsx, tests/client/ProjectFormDrawer.test.tsx</files>
  <behavior>
    - Test 1: parseScripts 404 displays "No package.json found at this path."
    - Test 2: parseScripts 400 malformed JSON displays "Could not read package.json. Check that the file is valid JSON."
    - Test 3: empty scripts displays "No scripts found" and disables submit.
    - Test 4: parse loading state displays "Looking for scripts..." and prevents duplicate parse requests.
  </behavior>
  <action>Add script-loading, empty, and error states matching the UI-SPEC copy. Use existing form error styling patterns from Phase 1 tests. Do not log directory paths or error bodies to the console. Ensure LIFE-04 missing-path/package errors are visible before the user attempts lifecycle actions.</action>
  <verify>
    <automated>npx vitest run tests/client/ProjectFormDrawer.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Form-level package/script errors are actionable and covered by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input to browser form | Directory path and selected script are entered or chosen by the user. |
| Browser form to parse-scripts API | Directory path is sent to server for package.json parsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | script dropdown | mitigate | Script options come from parseScripts response and submit requires one selected script per D-03. |
| T-02-05-02 | Information Disclosure | form error display | mitigate | Show controlled error messages from UI-SPEC, not raw server stack traces. |
| T-02-05-03 | Spoofing | path field | accept | v1 is trusted single-user local software; backend still validates package.json on start/restart. |
</threat_model>

<verification>
Run `npx vitest run tests/client/ProjectFormDrawer.test.tsx --reporter=verbose`, then `npx tsc --noEmit`.
</verification>

<success_criteria>
D-01 through D-04 and D-18 are implemented by executable form tasks, and the project form creates lifecycle-ready records with visible parse errors.
</success_criteria>

<output>
Create `.planning/phases/02-lifecycle-process-control/02-05-SUMMARY.md` when done.
</output>
