---
phase: 02-lifecycle-process-control
plan: 06
type: execute
wave: 5
depends_on:
  - 02-04
files_modified:
  - src/client/components/LogViewerDialog.tsx
  - src/client/components/ProjectRegistryPage.tsx
  - src/client/components/ProjectTable.tsx
  - src/client/components/ProjectMobileList.tsx
  - tests/client/LogViewerDialog.test.tsx
  - tests/client/ProjectRegistryPage.test.tsx
autonomous: true
requirements:
  - LIFE-04
must_haves:
  truths:
    - "D-15: User can open a per-project run history view."
    - "D-16: Log viewer displays stdout, stderr, start time, end time, exit code, and crash/error information."
    - "D-17: Log viewer loads data from GET /api/projects/:id/logs through the client API."
    - "LIFE-04: Command failures and startup errors are inspectable without opening project terminals."
  artifacts:
    - path: "src/client/components/LogViewerDialog.tsx"
      provides: "Material UI log viewer dialog with run history and output panel"
    - path: "tests/client/LogViewerDialog.test.tsx"
      provides: "Component tests for loading, empty, history, stdout/stderr, and error states"
    - path: "src/client/components/ProjectRegistryPage.tsx"
      provides: "Dialog state and log trigger wiring"
  key_links:
    - from: "src/client/components/LogViewerDialog.tsx"
      to: "src/client/api/projectsApi.ts"
      via: "getProjectLogs(projectId)"
      pattern: "getProjectLogs"
    - from: "src/client/components/ProjectTable.tsx"
      to: "src/client/components/LogViewerDialog.tsx"
      via: "onOpenLogs(project)"
      pattern: "onOpenLogs"
---

<objective>
Add the Phase 2 log viewer and wire it into the registry page.

Purpose: Complete D-15 through D-17 coverage by giving users a Material UI dialog for recent run history and current output.
Output: LogViewerDialog component, registry/table/mobile wiring, and component tests.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-lifecycle-process-control/02-CONTEXT.md
@.planning/phases/02-lifecycle-process-control/02-RESEARCH.md
@.planning/phases/02-lifecycle-process-control/02-PATTERNS.md
@.planning/phases/02-lifecycle-process-control/02-UI-SPEC.md
@.planning/phases/02-lifecycle-process-control/02-VALIDATION.md
@.planning/phases/02-lifecycle-process-control/02-04-SUMMARY.md
@AGENTS.md

Use Material UI Dialog patterns from `DeleteProjectDialog.tsx`. Use `LogData` and `RunRecord` from `src/shared/lifecycleSchema.ts`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LogViewerDialog with run history and output</name>
  <files>src/client/components/LogViewerDialog.tsx, tests/client/LogViewerDialog.test.tsx</files>
  <behavior>
    - Test 1: open dialog calls getProjectLogs(projectId).
    - Test 2: loading state renders while logs load.
    - Test 3: empty currentRun and empty history renders "No logs recorded for this project yet."
    - Test 4: history renders script name, start time, end time, duration, exit code, and crash/error label.
    - Test 5: current run output renders stdout and stderr, with stderr distinguishable by `[ERR]` prefix or error styling.
    - Test 6: API failure renders a controlled error message.
  </behavior>
  <action>Create `LogViewerDialog` with props `open`, `project`, and `onClose`, where `project` includes id and name. On open, call `getProjectLogs(project.id)`. Render a Material UI Dialog fullWidth with desktop maxWidth and mobile fullScreen behavior matching the UI-SPEC. Include sections titled "Run history" and "Output". Use a monospaced output panel with `role="log"` and `aria-live="polite"`. Display D-16 fields from each RunRecord: scriptName, startTime, endTime, exitCode, signalCode, crashed/error, stdout, and stderr. Do not implement WebSocket or SSE streaming in Phase 2.</action>
  <verify>
    <automated>npx vitest run tests/client/LogViewerDialog.test.tsx --reporter=verbose</automated>
  </verify>
  <done>LogViewerDialog displays D-15/D-16 log data and handles loading, empty, and error states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire log viewer trigger into registry surfaces</name>
  <files>src/client/components/ProjectRegistryPage.tsx, src/client/components/ProjectTable.tsx, src/client/components/ProjectMobileList.tsx, tests/client/ProjectRegistryPage.test.tsx</files>
  <behavior>
    - Test 1: each project row/card exposes a log viewer icon button with `aria-label="View logs for {project name}"`.
    - Test 2: clicking the log button opens LogViewerDialog for that project.
    - Test 3: closing the dialog clears selected project state.
    - Test 4: lifecycle controls from Plan 04 still render after adding the log action.
  </behavior>
  <action>Add a `Terminal` icon button to desktop and mobile action areas and pass `onOpenLogs(project)` up to `ProjectRegistryPage`. Add selected-log-project state and render `LogViewerDialog` once at page level. Preserve the Phase 2 action density: if the existing action row becomes cramped, use a tooltip-wrapped icon button rather than visible text. Keep Edit and Delete behavior unchanged.</action>
  <verify>
    <automated>npx vitest run tests/client/ProjectRegistryPage.test.tsx tests/client/LogViewerDialog.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Users can open and close per-project logs from both desktop and mobile registry surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client to logs API | Browser requests log history for a project id. |
| Logs API response to UI | Process stdout/stderr are rendered in the browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Information Disclosure | LogViewerDialog | accept | Logs are an intended local single-user feature and remain in memory only. |
| T-02-06-02 | Tampering | log output rendering | mitigate | Render log lines as React text nodes, never as HTML. |
| T-02-06-03 | Denial of Service | large output panel | mitigate | Backend ring buffer caps output; UI renders bounded arrays from LogData. |
</threat_model>

<verification>
Run `npx vitest run tests/client/LogViewerDialog.test.tsx tests/client/ProjectRegistryPage.test.tsx --reporter=verbose`, then `npx tsc --noEmit`.
</verification>

<success_criteria>
D-15 through D-17 are covered by executable frontend tasks, logs are inspectable from the registry page, and tests cover history, output, empty, and error states.
</success_criteria>

<output>
Create `.planning/phases/02-lifecycle-process-control/02-06-SUMMARY.md` when done.
</output>
