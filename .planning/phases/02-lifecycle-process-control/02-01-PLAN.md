---
phase: 02-lifecycle-process-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/projectSchema.ts
  - src/shared/lifecycleSchema.ts
  - src/server/process/packageJsonParser.ts
  - tests/shared/projectSchema.test.ts
  - tests/shared/lifecycleSchema.test.ts
  - tests/server/packageJsonParser.test.ts
autonomous: true
requirements:
  - LIFE-01
  - LIFE-04
must_haves:
  truths:
    - "D-18: Registered projects can store one enabled script name alongside the project path."
    - "D-02: A selected project directory can be parsed for package.json scripts."
    - "D-03: Shared contracts represent one selected script per project, not multiple active scripts."
    - "LIFE-04: Missing or malformed package.json files produce actionable typed errors."
  artifacts:
    - path: "src/shared/projectSchema.ts"
      provides: "Optional scriptName field on project input and stored project records per D-18"
      pattern: "scriptName"
    - path: "src/shared/lifecycleSchema.ts"
      provides: "Shared lifecycle, log, and package-script DTO schemas for client and server use"
      exports: ["processStateSchema", "processStatusSchema", "logDataSchema", "packageScriptsSchema", "parseScriptsResponseSchema"]
    - path: "src/server/process/packageJsonParser.ts"
      provides: "parsePackageJson() with typed missing-file and parse errors per D-02"
      exports: ["parsePackageJson", "PackageJsonNotFoundError", "PackageJsonParseError"]
    - path: "tests/server/packageJsonParser.test.ts"
      provides: "Package parser coverage for valid, missing, malformed, empty, and non-string scripts"
  key_links:
    - from: "src/server/process/packageJsonParser.ts"
      to: "src/shared/lifecycleSchema.ts"
      via: "PackageScripts type"
      pattern: "PackageScripts"
    - from: "src/shared/projectSchema.ts"
      to: "src/client/components/ProjectFormDrawer.tsx"
      via: "ProjectInput accepted by form submit payload in Plan 05"
      pattern: "scriptName"
---

## Multi-Source Coverage Audit

| Source | Item | Coverage in Plan Set |
|--------|------|----------------------|
| GOAL | Start dev servers from backend | Plans 02 and 03 |
| GOAL | Stop and restart managed projects | Plans 02 and 03 |
| GOAL | Failed commands surface actionable errors | Plans 01, 02, 03, 04, 05 |
| GOAL | Process state remains coherent | Plan 02 |
| REQ LIFE-01 | Start registered project's dev server from UI | Plans 01, 02, 03, 04, 05 |
| REQ LIFE-02 | Stop a running project from UI | Plans 02, 03, 04 |
| REQ LIFE-03 | Restart a running or failed project from UI | Plans 02, 03, 04 |
| REQ LIFE-04 | See command failures and missing-path errors | Plans 01, 02, 03, 04, 05, 06 |
| RESEARCH | Native child_process, no new package installs | Plan 02 |
| RESEARCH | Validate scriptName against package.json scripts before execution | Plans 01 and 03 |
| RESEARCH | Shared DTOs rather than frontend importing server process types | Plans 01, 02, 03, 04, 06 |
| CONTEXT | D-01 directory picker | Plan 05 |
| CONTEXT | D-02 package.json script parsing | Plans 01, 03, 05 |
| CONTEXT | D-03 exactly one selected script | Plans 01, 05 |
| CONTEXT | D-04 hide old Phase 1 fields in UI | Plans 05 and 04 |
| CONTEXT | D-05 child_process.spawn | Plan 02 |
| CONTEXT | D-06 process groups | Plan 02 |
| CONTEXT | D-07 in-memory ring buffer logs | Plan 02 |
| CONTEXT | D-08 lifecycle state machine | Plan 02 |
| CONTEXT | D-09 in-memory process registry | Plan 02 |
| CONTEXT | D-10 POST start | Plans 03 and 04 |
| CONTEXT | D-11 POST stop graceful then force | Plans 02 and 03 |
| CONTEXT | D-12 POST restart | Plans 03 and 04 |
| CONTEXT | D-13 GET status | Plans 03 and 04 |
| CONTEXT | D-14 immediate lifecycle responses plus polling | Plans 03 and 04 |
| CONTEXT | D-15 per-run log history | Plans 02, 03, 06 |
| CONTEXT | D-16 log data fields | Plans 01, 02, 06 |
| CONTEXT | D-17 GET logs | Plans 03, 04, 06 |
| CONTEXT | D-18 scriptName stored with path | Plans 01 and 05 |

<objective>
Create the shared Phase 2 lifecycle contracts, extend project records with `scriptName`, and implement package.json script parsing.

Purpose: Establish one client/server DTO source for lifecycle, log, and script responses; make the project schema store the selected script per D-18; and support D-02 script discovery before any process execution occurs.
Output: Updated shared schemas, parser utility, and focused tests.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-lifecycle-process-control/02-CONTEXT.md
@.planning/phases/02-lifecycle-process-control/02-RESEARCH.md
@.planning/phases/02-lifecycle-process-control/02-PATTERNS.md
@.planning/phases/02-lifecycle-process-control/02-VALIDATION.md
@AGENTS.md

Locked decisions implemented here: D-02, D-03, D-16, D-18.
Downstream plans must import lifecycle/log/package-script DTO types from `src/shared/lifecycleSchema.ts`, not from `src/server/process/*`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scriptName and shared lifecycle DTO contracts</name>
  <files>src/shared/projectSchema.ts, src/shared/lifecycleSchema.ts, tests/shared/projectSchema.test.ts, tests/shared/lifecycleSchema.test.ts</files>
  <behavior>
    - Test 1: projectInputSchema accepts a valid payload with `scriptName: "dev"`.
    - Test 2: projectInputSchema still accepts existing Phase 1 payloads without `scriptName`.
    - Test 3: lifecycleSchema accepts states stopped, starting, running, stopping, failed, and errored per D-08.
    - Test 4: processStatusSchema accepts state, uptime, currentRun, recentLogTail, and error fields per D-13.
    - Test 5: logDataSchema accepts currentRun plus history entries with stdout, stderr, start time, end time, exit code, signal code, and crash/error details per D-15 and D-16.
    - Test 6: packageScriptsSchema only accepts string-valued scripts.
  </behavior>
  <action>Add `scriptName: z.string().optional()` to `projectInputSchema` without removing any Phase 1 fields, satisfying D-04 schema compatibility and D-18 storage. Create `src/shared/lifecycleSchema.ts` with Zod schemas and exported TypeScript types for `ProcessState`, `RunRecord`, `ProcessStatus`, `LogData`, `PackageScripts`, and `ParseScriptsResponse`. The run record contract must include `runId`, `scriptName`, `startTime`, optional `endTime`, optional `exitCode`, optional `signalCode`, optional `error`, `stdout`, `stderr`, and optional boolean `crashed` so D-15 and D-16 are covered. Tests are written before implementation and assert both schema parsing and exported DTO shape through type-safe fixtures. Do not import from `src/server/process/*` in shared or client code.</action>
  <verify>
    <automated>npx vitest run tests/shared/projectSchema.test.ts tests/shared/lifecycleSchema.test.ts --reporter=verbose</automated>
  </verify>
  <done>`scriptName` is schema-backed, shared lifecycle DTOs exist, and client/server plans have a shared type source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement package.json script parser</name>
  <files>src/server/process/packageJsonParser.ts, tests/server/packageJsonParser.test.ts</files>
  <behavior>
    - Test 1: valid package.json with scripts returns `{ scripts, path }`.
    - Test 2: missing package.json throws `PackageJsonNotFoundError`.
    - Test 3: malformed package.json throws `PackageJsonParseError`.
    - Test 4: package.json without scripts returns an empty scripts object.
    - Test 5: non-string scripts are ignored.
  </behavior>
  <action>Create `src/server/process/packageJsonParser.ts` using Node built-ins `node:fs/promises` and `node:path`. Export `PackageJsonNotFoundError`, `PackageJsonParseError`, and `parsePackageJson(dirPath: string): Promise<ParseScriptsResponse>`. Resolve `dirPath` before appending `package.json`, read the file, parse JSON, return only string-valued scripts, and return the resolved package.json path. Use the shared `PackageScripts` and `ParseScriptsResponse` types from `src/shared/lifecycleSchema.ts`. Do not add dependencies. Implement tests using isolated temp directories only.</action>
  <verify>
    <automated>npx vitest run tests/server/packageJsonParser.test.ts --reporter=verbose</automated>
  </verify>
  <done>Package script discovery supports D-02 and produces typed actionable errors for LIFE-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to parse-scripts API | User-provided directory paths reach server-side filesystem reads. |
| Parser to filesystem | Server reads package.json at a user-selected path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `scriptName` project field | mitigate | D-18 stores scriptName, but Plan 03 validates it against package.json scripts immediately before start/restart. |
| T-02-01-02 | Information Disclosure | `packageJsonParser.ts` | mitigate | Parser reads only package.json and returns only script names/values, not env files or arbitrary directory contents. |
| T-02-01-03 | Denial of Service | package.json parsing | accept | Trusted single-user tool; parser reads one expected project metadata file. No external package install occurs. |
| T-02-01-SC | Tampering | npm installs | accept | Phase 2 adds no npm, pip, or cargo packages. |
</threat_model>

<verification>
Run `npx vitest run tests/shared/projectSchema.test.ts tests/shared/lifecycleSchema.test.ts tests/server/packageJsonParser.test.ts --reporter=verbose`, then `npx tsc --noEmit`.
</verification>

<success_criteria>
Shared DTO contracts exist, package.json parsing is tested, and D-02, D-03, D-16, and D-18 are explicitly covered without client imports from server process modules.
</success_criteria>

<output>
Create `.planning/phases/02-lifecycle-process-control/02-01-SUMMARY.md` when done.
</output>
