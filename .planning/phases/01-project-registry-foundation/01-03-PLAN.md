---
phase: 01-project-registry-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - index.html
  - src/client/main.tsx
  - src/client/App.tsx
  - src/client/theme.ts
  - src/server/index.ts
  - src/server/app.ts
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - REG-04
requirements_addressed:
  - REG-01
  - REG-02
  - REG-03
  - REG-04
must_haves:
  truths:
    - "The repository has a runnable TypeScript Node/Vite/React app shell."
    - "Material UI theming is established at the React root."
    - "The Express app factory exists for later registry routes."
    - "The first screen is a registry shell rather than a landing page."
  artifacts:
    - path: "src/client/App.tsx"
      provides: "first-screen registry placeholder shell"
    - path: "src/client/theme.ts"
      provides: "Material UI operational dashboard theme"
    - path: "src/server/app.ts"
      provides: "Express app factory for later registry routes"
    - path: "src/server/index.ts"
      provides: "server startup entry point"
  key_links:
    - from: "src/client/main.tsx"
      to: "src/client/theme.ts"
      via: "ThemeProvider"
      pattern: "ThemeProvider"
    - from: "src/server/index.ts"
      to: "src/server/app.ts"
      via: "createApp import"
      pattern: "createApp"
---

<objective>
Add the minimal React and Express app shell.

Purpose: Create the frontend/backend entry points that later registry API and UI plans extend, without mixing app shell work with tooling or feature implementation.
Output: Vite HTML entry, React root with Material UI theme, first-screen registry placeholder, Express `createApp()` factory, and server startup entry.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-registry-foundation/01-CONTEXT.md
@.planning/phases/01-project-registry-foundation/01-UI-SPEC.md
@.planning/phases/01-project-registry-foundation/01-RESEARCH.md
@.planning/phases/01-project-registry-foundation/01-01-SUMMARY.md
@.planning/phases/01-project-registry-foundation/01-02-SUMMARY.md
@AGENTS.md

Interfaces from earlier plans:
- `src/shared/projectSchema.ts` exports the Phase 1 project and registry contracts for downstream route/UI imports.

Locked decisions implemented here:
- D-10/D-11/D-12/D-13: The first screen must be the Material UI operational registry shell, not a landing page.
- D-06: Backend app shell must not log request bodies or env values.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add minimal app shell entry points</name>
  <read_first>
    <file>.planning/phases/01-project-registry-foundation/01-UI-SPEC.md</file>
    <file>.planning/phases/01-project-registry-foundation/01-RESEARCH.md</file>
  </read_first>
  <files>index.html, src/client/main.tsx, src/client/App.tsx, src/client/theme.ts, src/server/index.ts, src/server/app.ts</files>
  <action>Create the minimal React entry that wraps `<App />` in Material UI `ThemeProvider` and `CssBaseline`, using UI-SPEC colors and compact typography tokens. `App.tsx` should render the first-screen `Projects` shell with an `Add project` button placeholder but no fake data, lifecycle controls, logs, health polling, or port checks. Create an Express `createApp()` factory with JSON body limit and `/api/health` returning `{ ok: true }`, plus `src/server/index.ts` that starts the server from `PORT` defaulting to 3001. Do not log request bodies or env values per D-06.</action>
  <verify>
    <automated>npm run build && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>The app builds, the server app factory can be imported by later tests, and the first screen is a registry shell rather than a landing page.</acceptance_criteria>
  <done>Frontend and backend entry points exist and compile against the scaffolded toolchain and shared schema plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Express API | Untrusted form JSON will later cross into the backend. |
| Express API -> filesystem | YAML registry writes will later persist user-controlled configuration. |
| Registry data -> future command executor | Phase 1 stores command strings and paths but must not execute them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | `src/server/app.ts` | mitigate | Add JSON middleware without request-body logging; env values are not logged per D-06. |
| T-01-03-02 | Elevation of Privilege | `src/client/App.tsx` | mitigate | Do not add start/stop/restart controls or execute command strings in Phase 1. |
| T-01-03-03 | Denial of Service | `src/server/app.ts` | mitigate | Use a small Express JSON body limit suitable for registry mutations. |
| T-01-03-04 | Spoofing | app shell | accept | v1 is trusted single-user local software and no auth/session surface is introduced in Phase 1. |
</threat_model>

<verification>
Run `npm run build && npm test -- --run`. Confirm there are no lifecycle controls, logs, status polling, port checks, Docker runtime implementation, or autostart execution in generated source.
</verification>

<success_criteria>
- App shell compiles with Material UI and Express.
- First screen is a registry-oriented `Projects` shell.
- Express app factory is ready for Plan 04 route mounting.
- No later-phase lifecycle or Docker runtime behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-registry-foundation/01-03-SUMMARY.md`.
</output>
