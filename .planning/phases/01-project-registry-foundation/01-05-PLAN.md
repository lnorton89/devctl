---
phase: 01-project-registry-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - src/client/App.tsx
  - src/client/api/projectsApi.ts
  - src/client/components/ProjectRegistryPage.tsx
  - src/client/components/ProjectTable.tsx
  - src/client/components/ProjectMobileList.tsx
  - tests/client/ProjectRegistryPage.test.tsx
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
requirements_addressed:
  - REG-01
  - REG-02
  - REG-03
must_haves:
  truths:
    - "User can see registered projects on the first screen."
    - "The registry page can load projects and represent empty, loading, and load-error states."
    - "Desktop and narrow viewport registry displays expose required project metadata."
    - "Environment variable values are not visible in registry list/table surfaces."
  artifacts:
    - path: "src/client/api/projectsApi.ts"
      provides: "typed API client for registry endpoints"
    - path: "src/client/components/ProjectRegistryPage.tsx"
      provides: "main registry workflow orchestration"
    - path: "src/client/components/ProjectTable.tsx"
      provides: "desktop dense Material UI table"
    - path: "src/client/components/ProjectMobileList.tsx"
      provides: "responsive compact registry list"
  key_links:
    - from: "src/client/components/ProjectRegistryPage.tsx"
      to: "src/client/api/projectsApi.ts"
      via: "fetch CRUD calls"
      pattern: "projectsApi"
    - from: "src/client/components/ProjectTable.tsx"
      to: "src/shared/projectSchema.ts"
      via: "ProjectConfig type import"
      pattern: "ProjectConfig"
    - from: "src/client/App.tsx"
      to: "src/client/components/ProjectRegistryPage.tsx"
      via: "first-screen page render"
      pattern: "ProjectRegistryPage"
---

<objective>
Build the registry page state flow and display surfaces.

Purpose: Deliver the first-screen Material UI registry dashboard that reads from the API and presents project metadata without sensitive env value leakage.
Output: Typed API client, registry page shell, desktop table, responsive compact list, and page-level component tests.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-registry-foundation/01-CONTEXT.md
@.planning/phases/01-project-registry-foundation/01-UI-SPEC.md
@.planning/phases/01-project-registry-foundation/01-RESEARCH.md
@.planning/phases/01-project-registry-foundation/01-02-SUMMARY.md
@.planning/phases/01-project-registry-foundation/01-04-SUMMARY.md
@AGENTS.md

Interfaces from earlier plans:
- Shared types/schemas: import `ProjectConfig`, `ProjectInput`, and validation helpers from `src/shared/projectSchema.ts`.
- API endpoints: `GET /api/projects`, `POST /api/projects`, `PUT /api/projects/:id`, `DELETE /api/projects/:id`.

Locked decisions implemented here:
- D-01/D-02: Both Host path and Container path must be visible in table/list.
- D-04/D-06: Env values must not appear in registry table/list surfaces.
- D-10/D-12/D-13: Dense Material UI desktop table, responsive compact mobile layout, accessible controls.

Phase boundaries:
- Do not implement start, stop, restart, live lifecycle status, logs, health polling, port occupancy checks, or autostart execution.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed client API and registry page state flow</name>
  <read_first>
    <file>src/shared/projectSchema.ts</file>
    <file>src/server/routes/projects.ts</file>
    <file>.planning/phases/01-project-registry-foundation/01-UI-SPEC.md</file>
  </read_first>
  <files>src/client/api/projectsApi.ts, src/client/App.tsx, src/client/components/ProjectRegistryPage.tsx, tests/client/ProjectRegistryPage.test.tsx</files>
  <action>Implement a typed `projectsApi` wrapper around `fetch` for list/create/update/delete, mapping API validation issues and load/save errors into client-friendly errors. Build `ProjectRegistryPage` as the first screen with title `Projects`, primary CTA `Add project`, loading state, empty state copy from UI-SPEC, load error copy, save error snackbar/alert copy, and state placeholders for create/edit/delete workflows that Plan 06 will complete. `App.tsx` should render this page directly. Tests should mock the API module and verify first-screen rendering, empty state replacement after loaded projects, load error copy, primary CTA presence, and that no lifecycle controls are rendered.</action>
  <verify>
    <automated>npm test -- tests/client/ProjectRegistryPage.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>The registry page fetches projects on mount, shows loading/empty/error states, exposes add/edit/delete action hooks for Plan 06, and does not show start/stop/restart/log/status controls.</acceptance_criteria>
  <done>Client API and page state orchestration exist and are covered by component tests.</done>
</task>

<task type="auto">
  <name>Task 2: Build desktop and responsive registry displays</name>
  <read_first>
    <file>.planning/phases/01-project-registry-foundation/01-UI-SPEC.md</file>
    <file>src/shared/projectSchema.ts</file>
  </read_first>
  <files>src/client/components/ProjectTable.tsx, src/client/components/ProjectMobileList.tsx, src/client/components/ProjectRegistryPage.tsx, tests/client/ProjectRegistryPage.test.tsx</files>
  <action>Implement a dense Material UI desktop table with the required columns: Project, Host path, Container path, Command, Port, Health URL, Autostart, Actions. Use monospace styling for paths, command, port, and URLs where useful. Truncate long command/path values with `Tooltip`. Add edit/delete icon buttons with exact accessible labels `Edit project` and `Delete project`, wired to callbacks passed from `ProjectRegistryPage`; Plan 06 will implement the drawer/dialog opened by these callbacks. For narrow screens, render compact responsive rows/cards with the same essential metadata and action clarity. Do not display env variable values in either registry display; an env count or omission is acceptable. Keep spacing and typography aligned with UI-SPEC tokens and avoid nested cards.</action>
  <verify>
    <automated>npm test -- tests/client/ProjectRegistryPage.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>Desktop table and mobile compact layout expose required project fields and accessible actions while omitting sensitive env values and lifecycle controls.</acceptance_criteria>
  <done>Registry display components match the UI contract and are wired into the page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React client -> Express API | Registry reads and future mutations cross into backend validation. |
| API response -> registry table/list | Persisted data is displayed and must avoid leaking env values in table/list views. |
| User action -> future edit/delete surfaces | Edit/delete action selection is local UI state until Plan 06 completes mutation surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Information Disclosure | `ProjectTable.tsx` / `ProjectMobileList.tsx` | mitigate | Never render env variable values outside the Plan 06 form surface. |
| T-01-05-02 | Tampering | `projectsApi.ts` | mitigate | Route mutations through typed API helpers and preserve backend validation issue paths for form handling. |
| T-01-05-03 | Elevation of Privilege | `ProjectRegistryPage.tsx` | mitigate | Do not render lifecycle execution controls in Phase 1. |
| T-01-05-04 | Spoofing | UI actions | accept | v1 is trusted single-user local software; no authentication is in scope. |
</threat_model>

<verification>
Run `npm test -- tests/client/ProjectRegistryPage.test.tsx --run` and `npm run build`.
</verification>

<success_criteria>
- REG-01/REG-03 project fields render in first-screen registry displays.
- REG-02 edit/delete row actions are visible and accessible, with mutation surfaces deferred to Plan 06 rather than omitted.
- Env values are absent from table/list output.
- UI-SPEC first-screen operational dashboard requirements are met.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-registry-foundation/01-05-SUMMARY.md`.
</output>
