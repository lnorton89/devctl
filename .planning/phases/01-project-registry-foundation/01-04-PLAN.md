---
phase: 01-project-registry-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - tests/integration/registryFlow.test.ts
  - README.md
  - .planning/phases/01-project-registry-foundation/01-VERIFICATION.md
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - REG-04
requirements_addressed:
  - REG-01
  - REG-02
  - REG-03
  - REG-04
must_haves:
  truths:
    - "An integration test proves API registry CRUD persists through YAML reload."
    - "Project documentation explains how to run the Phase 1 app and where YAML config is stored."
    - "Verification confirms Phase 1 does not include later lifecycle features."
    - "REG-01 through REG-04 have explicit evidence."
  artifacts:
    - path: "tests/integration/registryFlow.test.ts"
      provides: "end-to-end backend persistence flow"
    - path: "README.md"
      provides: "Phase 1 run/config notes"
    - path: ".planning/phases/01-project-registry-foundation/01-VERIFICATION.md"
      provides: "phase evidence mapped to requirements and locked decisions"
  key_links:
    - from: "tests/integration/registryFlow.test.ts"
      to: "src/server/app.ts"
      via: "Supertest against createApp"
      pattern: "createApp"
    - from: "tests/integration/registryFlow.test.ts"
      to: "data/projects.yaml behavior"
      via: "temporary DEVCTL_CONFIG_PATH"
      pattern: "DEVCTL_CONFIG_PATH"
    - from: "README.md"
      to: "package.json"
      via: "documented npm scripts"
      pattern: "npm run dev"
---

<objective>
Add integration verification and Phase 1 operating notes.

Purpose: Close the loop on REG-01 through REG-04 with executable evidence and concise documentation while preserving Phase 1 boundaries.
Output: Integration test, README run/config guidance, and phase verification record.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-registry-foundation/01-CONTEXT.md
@.planning/phases/01-project-registry-foundation/01-UI-SPEC.md
@.planning/phases/01-project-registry-foundation/01-RESEARCH.md
@.planning/phases/01-project-registry-foundation/01-02-SUMMARY.md
@.planning/phases/01-project-registry-foundation/01-03-SUMMARY.md
@AGENTS.md

Use this plan to verify, document, and record evidence only. Do not add lifecycle controls, Docker runtime implementation, logs, health polling, port occupancy checks, or autostart execution.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add registry flow integration test</name>
  <read_first>
    <file>src/server/app.ts</file>
    <file>src/server/routes/projects.ts</file>
    <file>src/server/registry/registryRepository.ts</file>
    <file>src/shared/projectSchema.ts</file>
  </read_first>
  <files>tests/integration/registryFlow.test.ts</files>
  <behavior>
    - Create a temporary YAML registry path.
    - POST a project containing all Phase 1 fields, including env key/value, envFilePath, appUrl, port, healthUrl, and autostart.
    - GET returns the created project.
    - PUT updates editable fields.
    - DELETE removes the project.
    - A new app/repository instance reading the same temp YAML sees saved data before delete.
  </behavior>
  <action>Write an integration test using Supertest and a temp directory. The test must use a dedicated registry path, not the real `data/projects.yaml`. Exercise create/list/update/persistence/delete through HTTP routes, including all REG-01 and REG-03 fields. Assert the YAML file exists and is human-readable enough to contain `version: 1` and project fields. Do not snapshot or print env values; direct assertions in memory are acceptable.</action>
  <verify>
    <automated>npm test -- tests/integration/registryFlow.test.ts --run</automated>
  </verify>
  <acceptance_criteria>The integration test proves REG-01 through REG-04 through the API and YAML persistence boundary without touching real user config.</acceptance_criteria>
  <done>Integration test passes and can be run with the normal Vitest suite.</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 1 run and config behavior</name>
  <read_first>
    <file>package.json</file>
    <file>.planning/phases/01-project-registry-foundation/01-CONTEXT.md</file>
    <file>.planning/phases/01-project-registry-foundation/01-UI-SPEC.md</file>
  </read_first>
  <files>README.md</files>
  <action>Create a concise README covering what devctl is, the current Phase 1 registry scope, how to install/run/test/build, the default YAML registry path, how `DEVCTL_CONFIG_PATH` overrides it, and what fields are stored. Explicitly state that Phase 1 stores command, path, env, and autostart configuration but does not execute commands, start/stop projects, poll health, check ports, show logs, or run autostart. Mention that env values are local sensitive configuration and are editable in the form, not shown in the registry table.</action>
  <verify>
    <automated>npm run build && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>README accurately reflects implemented scripts and Phase 1 boundaries without adding future-phase instructions as if implemented.</acceptance_criteria>
  <done>README gives the user enough to run and verify Phase 1 locally.</done>
</task>

<task type="auto">
  <name>Task 3: Record Phase 1 verification evidence</name>
  <read_first>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/phases/01-project-registry-foundation/01-CONTEXT.md</file>
    <file>.planning/phases/01-project-registry-foundation/01-UI-SPEC.md</file>
  </read_first>
  <files>.planning/phases/01-project-registry-foundation/01-VERIFICATION.md</files>
  <action>Create `01-VERIFICATION.md` mapping REG-01 through REG-04 to the implementation files and automated commands that prove each one. Include a locked-decision coverage table for D-01 through D-13 and mark deferred ideas as none. Include a negative-scope checklist confirming no lifecycle controls, logs, health polling, port occupancy checks, Docker runtime implementation, or autostart execution were added. Run the full verification command and record the result.</action>
  <verify>
    <automated>npm run build && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>Verification document contains requirement evidence, decision coverage, negative-scope checks, and passing command results.</acceptance_criteria>
  <done>Phase 1 has a durable evidence record for execute/verify workflows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test client -> API | Integration tests send representative untrusted registry data. |
| API -> temp YAML file | Tests exercise durable persistence without touching real user config. |
| Documentation -> user operation | README must not imply unsafe lifecycle or Docker host-control behavior exists yet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | `registryFlow.test.ts` | mitigate | Use isolated temp registry paths and assert malformed/invalid protections are already covered by repository tests. |
| T-01-04-02 | Information Disclosure | `registryFlow.test.ts` / `README.md` | mitigate | Do not snapshot or print env values; document env values as sensitive local config. |
| T-01-04-03 | Elevation of Privilege | `README.md` | mitigate | State that commands are stored only and are not executed in Phase 1. |
| T-01-04-04 | Denial of Service | Test suite | accept | Integration tests use small local fixtures and temp files only. |
</threat_model>

<verification>
Run `npm run build && npm test -- --run`. Review `README.md` and `01-VERIFICATION.md` for explicit exclusion of lifecycle features.
</verification>

<success_criteria>
- REG-01 through REG-04 have automated integration evidence.
- README documents Phase 1 scope and persistence path clearly.
- Verification file maps requirements, locked decisions, and negative scope.
- Full build and test suite pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-registry-foundation/01-04-SUMMARY.md`.
</output>
