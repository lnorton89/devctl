---
phase: 01-project-registry-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - tsconfig.node.json
  - vite.config.ts
  - vitest.config.ts
  - index.html
  - src/client/main.tsx
  - src/client/App.tsx
  - src/client/theme.ts
  - src/server/index.ts
  - src/server/app.ts
  - src/shared/projectSchema.ts
  - tests/setup.ts
  - tests/shared/projectSchema.test.ts
  - data/.gitkeep
  - .gitignore
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - REG-04
requirements_addressed:
  - REG-01
  - REG-02
  - REG-03
  - REG-04
must_haves:
  truths:
    - "The repository has a runnable TypeScript Node/Vite/React app shell."
    - "Material UI theming is established at the React root."
    - "The shared project schema names all Phase 1 registry fields."
    - "Tests can run before feature implementation expands."
  artifacts:
    - path: "package.json"
      provides: "npm scripts and exact dependency manifest"
    - path: "src/client/App.tsx"
      provides: "first-screen registry placeholder shell"
    - path: "src/server/app.ts"
      provides: "Express app factory for later registry routes"
    - path: "src/shared/projectSchema.ts"
      provides: "shared Zod schema contract"
    - path: "vitest.config.ts"
      provides: "test runner setup for shared/server/client tests"
  key_links:
    - from: "src/client/main.tsx"
      to: "src/client/theme.ts"
      via: "ThemeProvider"
      pattern: "ThemeProvider"
    - from: "src/server/index.ts"
      to: "src/server/app.ts"
      via: "createApp import"
      pattern: "createApp"
    - from: "tests/shared/projectSchema.test.ts"
      to: "src/shared/projectSchema.ts"
      via: "schema import"
      pattern: "projectInputSchema"
---

<objective>
Scaffold the app foundation for Phase 1.

Purpose: Establish the TypeScript, React, Material UI, Express, shared schema, and Vitest foundation that all later registry plans build on.
Output: A fresh app shell with installable dependencies, build/test scripts, a minimal backend app factory, and a shared project schema stub covering REG-01 through REG-04 fields.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-registry-foundation/01-CONTEXT.md
@.planning/phases/01-project-registry-foundation/01-UI-SPEC.md
@.planning/phases/01-project-registry-foundation/01-RESEARCH.md
@AGENTS.md

Important locked decisions:
- D-01/D-02/D-03: Store both hostPath and containerPath; both are required for unambiguous future execution.
- D-04/D-05/D-06: Support env key/value entries and optional envFilePath; do not casually log or table-display env values.
- D-07/D-08/D-09: YAML persistence is required later; this plan only creates the dependency and schema foundation.
- D-10/D-11/D-12/D-13: UI must be Material UI, dense, operational, and registry-first.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package and toolchain configuration</name>
  <read_first>
    <file>.planning/phases/01-project-registry-foundation/01-RESEARCH.md</file>
    <file>AGENTS.md</file>
  </read_first>
  <files>package.json, package-lock.json, tsconfig.json, tsconfig.node.json, vite.config.ts, vitest.config.ts, tests/setup.ts, .gitignore, data/.gitkeep</files>
  <action>Create a strict TypeScript app manifest using the researched stack: Vite, React, React DOM, Material UI, MUI icons, Emotion, Express, yaml, zod, Vitest, Testing Library, jsdom, Supertest, and Node/Express/Supertest types. Add scripts for `dev`, `dev:client`, `dev:server`, `build`, `preview`, `test`, and `typecheck`. Configure Vite to serve the React app and proxy `/api` to the Express dev server port. Configure Vitest for TypeScript with jsdom available for client tests and setup file importing `@testing-library/jest-dom`. Add `data/.gitkeep` because REG-04 persistence will use a YAML file under a durable data directory in later plans. Run `npm install` so `package-lock.json` is created. Do not add Docker runtime files or lifecycle scripts in this phase.</action>
  <verify>
    <automated>npm install && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Dependencies install cleanly, TypeScript configuration loads, and scripts exist for build/test/dev workflows without lifecycle or Docker execution scope.</acceptance_criteria>
  <done>`package.json`, lockfile, TS/Vite/Vitest config, test setup, ignore rules, and data directory placeholder exist and typecheck succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add shared schema contract and smoke tests</name>
  <read_first>
    <file>.planning/phases/01-project-registry-foundation/01-CONTEXT.md</file>
    <file>.planning/phases/01-project-registry-foundation/01-UI-SPEC.md</file>
  </read_first>
  <files>src/shared/projectSchema.ts, tests/shared/projectSchema.test.ts</files>
  <behavior>
    - Required fields per REG-01 and D-01/D-03: name, hostPath, containerPath, startCommand.
    - Optional fields per REG-01/REG-03/D-04/D-05: appUrl, port, healthUrl, envFilePath, env, autostart.
    - Defaults: env is an empty array and autostart is false.
    - Validation copy-compatible rules: port is 1 through 65535; URLs must be valid when present; env keys use letters, numbers, and underscores.
  </behavior>
  <action>Write tests first for the schema behaviors above, then implement `src/shared/projectSchema.ts` using Zod. Export `projectInputSchema`, `projectSchema`, `registrySchema`, `ProjectInput`, `ProjectConfig`, `RegistryFile`, and a small helper for formatting Zod issues into `{ path, message }` objects that API/UI plans can reuse. Include a top-level registry `version: 1` schema for YAML persistence. Avoid any command execution or path existence checks; Phase 1 validates shape only per D-03.</action>
  <verify>
    <automated>npm test -- tests/shared/projectSchema.test.ts --run</automated>
  </verify>
  <acceptance_criteria>Schema tests prove required path/command fields, optional registry fields, env key validation, URL/port validation, and defaults.</acceptance_criteria>
  <done>Shared schema exports are stable enough for backend and frontend plans to import without rediscovering the project model.</done>
</task>

<task type="auto">
  <name>Task 3: Add minimal app shell entry points</name>
  <read_first>
    <file>.planning/phases/01-project-registry-foundation/01-UI-SPEC.md</file>
    <file>.planning/phases/01-project-registry-foundation/01-RESEARCH.md</file>
  </read_first>
  <files>index.html, src/client/main.tsx, src/client/App.tsx, src/client/theme.ts, src/server/index.ts, src/server/app.ts</files>
  <action>Create the minimal React entry that wraps `<App />` in Material UI `ThemeProvider` and `CssBaseline`, using UI-SPEC colors and compact typography tokens. `App.tsx` should render the first-screen `Projects` shell with an `Add project` button placeholder but no fake data, lifecycle controls, logs, health polling, or port checks. Create an Express `createApp()` factory with JSON body limit and `/api/health` returning `{ ok: true }`, plus `src/server/index.ts` that starts the server from `PORT` defaulting to 3001. Do not log request bodies or env values per D-06.</action>
  <verify>
    <automated>npm run build && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>The app builds, the server app factory can be imported by later tests, and the first screen is a registry shell rather than a landing page.</acceptance_criteria>
  <done>Frontend and backend entry points exist and compile against the scaffolded toolchain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Express API | Untrusted form JSON will later cross into the backend. |
| Express API -> filesystem | YAML registry writes will later persist user-controlled configuration. |
| Registry data -> future command executor | Phase 1 stores command strings and paths but must not execute them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | `src/shared/projectSchema.ts` | mitigate | Define shared Zod validation for all registry fields before API/persistence uses them. |
| T-01-01-02 | Information Disclosure | `src/server/app.ts` | mitigate | Add JSON middleware without request-body logging; env values are not logged per D-06. |
| T-01-01-03 | Elevation of Privilege | `src/client/App.tsx` | mitigate | Do not add start/stop/restart controls or execute command strings in Phase 1. |
| T-01-01-04 | Denial of Service | `src/server/app.ts` | mitigate | Use a small Express JSON body limit suitable for registry mutations. |
</threat_model>

<verification>
Run `npm run build && npm test -- --run`. Confirm there are no lifecycle controls, logs, status polling, port checks, or autostart execution in generated source.
</verification>

<success_criteria>
- REG-01/REG-03 fields exist in the shared schema contract.
- REG-02-compatible stable IDs are represented in project records.
- REG-04 persistence dependencies and data directory foundation exist.
- App shell compiles with Material UI and Express.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-registry-foundation/01-01-SUMMARY.md`.
</output>
