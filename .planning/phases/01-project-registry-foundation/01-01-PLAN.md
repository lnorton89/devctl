---
phase: 01-project-registry-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - tsconfig.node.json
  - vite.config.ts
  - vitest.config.ts
  - tests/setup.ts
  - data/.gitkeep
  - .gitignore
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - REG-04
requirements_addressed:
  - REG-01
  - REG-02
  - REG-03
  - REG-04
must_haves:
  truths:
    - "The repository has installable TypeScript, Vite, React, Material UI, Express, YAML, Zod, and Vitest tooling."
    - "Build, test, typecheck, client dev, and server dev scripts exist before feature implementation expands."
    - "A durable data directory placeholder exists for later YAML registry persistence."
  artifacts:
    - path: "package.json"
      provides: "npm scripts and dependency manifest"
    - path: "package-lock.json"
      provides: "locked dependency graph"
    - path: "vite.config.ts"
      provides: "Vite React dev/build configuration with API proxy"
    - path: "vitest.config.ts"
      provides: "test runner setup for shared/server/client tests"
    - path: "data/.gitkeep"
      provides: "registry data directory placeholder"
  key_links:
    - from: "package.json"
      to: "vitest.config.ts"
      via: "test script"
      pattern: "vitest"
    - from: "vite.config.ts"
      to: "src/server/index.ts"
      via: "dev proxy target reserved for later app shell"
      pattern: "/api"
---

<objective>
Create the Phase 1 toolchain and data-directory foundation.

Purpose: Establish the installable Node/Vite/React/MUI/Express/YAML/Zod/Vitest project baseline that all later registry plans build on while keeping this setup plan below the scope-sanity threshold.
Output: npm manifest and lockfile, strict TypeScript/Vite/Vitest config, Testing Library setup, ignore rules, and durable data directory placeholder.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-registry-foundation/01-CONTEXT.md
@.planning/phases/01-project-registry-foundation/01-UI-SPEC.md
@.planning/phases/01-project-registry-foundation/01-RESEARCH.md
@.planning/phases/01-project-registry-foundation/01-VALIDATION.md
@AGENTS.md

Important locked decisions:
- D-07/D-08/D-09: YAML persistence is required later; this plan creates the dependency and data directory foundation only.
- D-10/D-11/D-12/D-13: UI must be Material UI, dense, operational, and registry-first in downstream plans.

Phase boundaries:
- Do not add Docker runtime files, lifecycle scripts, command execution, start/stop/restart behavior, logs, health polling, port checks, or autostart execution in this plan.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package and TypeScript toolchain configuration</name>
  <read_first>
    <file>.planning/phases/01-project-registry-foundation/01-RESEARCH.md</file>
    <file>.planning/phases/01-project-registry-foundation/01-VALIDATION.md</file>
    <file>AGENTS.md</file>
  </read_first>
  <files>package.json, package-lock.json, tsconfig.json, tsconfig.node.json, vite.config.ts, vitest.config.ts, tests/setup.ts, data/.gitkeep, .gitignore</files>
  <action>Create a strict TypeScript app manifest using the researched stack: Vite, React, React DOM, Material UI, MUI icons, Emotion, Express, yaml, zod, Vitest, Testing Library, jsdom, Supertest, and Node/Express/Supertest types. Add scripts for `dev`, `dev:client`, `dev:server`, `build`, `preview`, `test`, and `typecheck`. Configure Vite to serve the future React app and proxy `/api` to the future Express dev server port. Configure Vitest for TypeScript with jsdom available for client tests and setup file importing `@testing-library/jest-dom`. Add `data/.gitkeep` because REG-04 persistence will use a YAML file under a durable data directory in later plans. Run `npm install` so `package-lock.json` is created. Do not add Docker runtime files or lifecycle scripts in this phase.</action>
  <verify>
    <automated>npm install && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Dependencies install cleanly, TypeScript configuration loads, and scripts exist for build/test/dev workflows without lifecycle or Docker execution scope.</acceptance_criteria>
  <done>`package.json`, lockfile, TS/Vite/Vitest config, test setup, ignore rules, and data directory placeholder exist and typecheck succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> future Express API | Future untrusted form JSON will cross into the backend. |
| Express API -> filesystem | Future YAML registry writes will persist user-controlled configuration. |
| Registry data -> future command executor | Phase 1 stores command strings and paths but must not execute them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | `package.json` / `vitest.config.ts` | mitigate | Install Zod and test tooling before schema/API plans so validation is automated at the boundaries. |
| T-01-01-02 | Information Disclosure | `tests/setup.ts` / test tooling | mitigate | Do not configure request/body logging helpers that could print env values. |
| T-01-01-03 | Elevation of Privilege | `package.json` scripts | mitigate | Scripts only run dev/build/test tooling; no project command execution scripts are added in Phase 1. |
| T-01-01-04 | Denial of Service | future API body parsing | mitigate | Include Express dependency for later small JSON body-limit configuration; body parsing itself is added in Plan 03. |
</threat_model>

<verification>
Run `npm install && npm run typecheck`. Confirm there are no lifecycle controls, logs, status polling, port checks, Docker runtime files, or autostart execution scripts in generated files.
</verification>

<success_criteria>
- Tooling dependencies and scripts needed for REG-01 through REG-04 implementation exist.
- REG-04 persistence has a durable data directory placeholder.
- Future plans can create schema, backend, frontend, and tests without changing the baseline package/tooling scope.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-registry-foundation/01-01-SUMMARY.md`.
</output>
